# Approved differences between the platform twins of this blueprint, checked by
# bin/check_twin_diff.py. One line per file: the hash of the difference, what kind of
# difference it is, the file, and why it exists. Write the reason - the file is read by
# people deciding whether a difference is the platform or a mistake.

31120fb94c65de07  only-springboot  src/main/java/blueprint/workflowmodule/Application.java  # one platform boots from a class of the application, the other one needs none
d8eaef3a339803ef  differs  src/main/java/blueprint/workflowmodule/loanapproval/ApiController.java  # the same GET endpoints, written with the HTTP annotations of the platform
d3e240be25066a9f  differs  src/main/java/blueprint/workflowmodule/loanapproval/Service.java  # bean and injection annotations, the configuration accessor, and the transaction reading an entity needs on one platform
480889f2ad9fb1a1  differs  src/main/java/blueprint/workflowmodule/loanapproval/Workflow.java  # bean and injection annotations only; both inject ProcessService here and nowhere else
6f779c401afdd136  differs  src/main/java/blueprint/workflowmodule/loanapproval/WorkflowTaskHandler.java  # bean and injection annotations only; the VanillaBP annotations are identical, which is the point
4f6d9798ac729e02  differs  src/main/java/blueprint/workflowmodule/loanapproval/config/LoanApprovalProperties.java  # configuration is bound to a class on one platform and to an interface on the other
6dcdfbe2838c4f44  differs  src/main/java/blueprint/workflowmodule/loanapproval/model/AggregateRepository.java  # each platform's repository idiom
9fb1f8af132dae40  differs  src/test/java/blueprint/workflowmodule/ApplicationSmokeTest.java  # test harness: the beans are asked for in the bean container's own words
33289af19edd6ccf  differs  src/test/java/blueprint/workflowmodule/WorkflowModuleTest.java  # test harness: polling reads the aggregate through the platform's own way of opening a transaction
0dab76bdffcefe7c  differs  src/test/java/blueprint/workflowmodule/loanapproval/LoanApprovalIT.java  # injection annotations, the test class carries @QuarkusTest itself because inheriting it leaves it without a bean, and the aggregate is read through the repository idiom of the platform
