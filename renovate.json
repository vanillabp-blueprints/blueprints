{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    "github>vanillabp/camunda8-adapter//renovate/camunda8-lines.json"
  ],
  "timezone": "Europe/Vienna",
  "prConcurrentLimit": 5,
  "labels": ["dependencies"],
  "minimumReleaseAge": "3 days",
  "automerge": true,
  "platformAutomerge": true,
  "automergeStrategy": "squash",
  "packageRules": [
    {
      "description": "VanillaBP and the BPMS adapters follow the framework, not Renovate. Their versions are bumped when a release is cut, and while they are snapshots there is nothing to look up. Once the adapters are released the Camunda 8 preset extended above takes over, and it keeps a blueprint inside the release line it was written for: the version suffix (2.1.0-8.9) is the lowest cluster version the adapter accepts, and Renovate never proposes an update that changes it.",
      "matchPackageNames": ["/^io\\.vanillabp/", "/^org\\.camunda\\.community\\.vanillabp/"],
      "enabled": false
    },
    {
      "description": "One PR per dependency across all blueprints. The same version is repeated in every blueprint POM on purpose, so an upgrade has to touch all of them at once or they drift apart.",
      "matchManagers": ["maven"],
      "groupName": "{{{depName}}}"
    },
    {
      "description": "The versions table of CONTRIBUTING.md names Spring Boot and Quarkus. A bump of either has to be carried into that table by hand, so it gets a PR to look at rather than an automerge.",
      "matchPackageNames": [
        "org.springframework.boot:spring-boot-starter-parent",
        "io.quarkus.platform:quarkus-bom"
      ],
      "automerge": false,
      "addLabels": ["needs-doc-update"]
    }
  ]
}
